# Agent Notes

- Claude local skills live in `~/.claude/skills/`. Other agents (Codex, Gemini) can reference these skill files directly.
- Project-specific guidance is in `CLAUDE.md`.
- Specs live under `resources/specs/` with `yymmdd-description.md` prefix.
